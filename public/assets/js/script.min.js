const imageContainers=document.querySelectorAll(".image-home-container");imageContainers.forEach(container=>{const titleContainer=container.querySelector(".image-title-container");container.addEventListener("mouseenter",()=>{titleContainer.style.visibility="visible"});container.addEventListener("mouseleave",()=>{titleContainer.style.visibility="hidden"})});const textarea=document.querySelector(".auto-resize");function autoResizeTextarea(element){element.style.height="auto";element.style.height=element.scrollHeight+"px"}textarea.addEventListener("input",function(){autoResizeTextarea(this)});autoResizeTextarea(textarea);p;