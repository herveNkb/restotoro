<nav class="navbar navbar-expand-lg  navbar-light
     pt-4
     px-4
     px-md-5
     pt-md-5
     px-lg-4
     pt-lg-4"
>
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_main') }}">
            <img
                    src="{{ asset('assets/img/logo/logo.png') }}"
                    alt="Logo du restaurant"
                    class="nav-logo">
        </a>
        <button
                class="navbar-toggler"
                type="button"
                data-bs-toggle="collapse"
                data-bs-target="#navbarNav"
                aria-controls="navbarNav"
                aria-expanded="false"
                aria-label="Toggle navigation"
        >
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse text-center mt-5  mt-lg-2" id="navbarNav">
            {# La classe ms-auto positionne le menu de navigation complétement à droite #}
            <ul class="navbar-nav ms-auto">
                <li class="nav-item pb-3 pb-lg-0 pe-lg-2">
                    <a class="nav-link" aria-current="page" href="#">Accueil</a>
                </li>
                <li class="nav-item pb-3 pb-lg-0 pe-lg-2">
                    <a class="nav-link" href="#">Notre carte</a>
                </li>
                <li class="nav-item pb-3 pb-lg-0 pe-lg-2">
                    <a class="nav-link" href="#">Nos menus</a>
                </li>
                <li class="nav-item pb-3 pb-lg-0 pe-lg-2">
                    <a class="nav-link" href="#">Réservation</a>
                </li>
                {# Quand l'utilisateur est connecté les deux liens suivants sont apparents #}
                {% if app.user %}
                    <li class="nav-item pb-3 pb-lg-0 pe-lg-2">
                        <a class="nav-link" href="#">Mon compte</a>
                    </li>
                    <li class="nav-item pb-3 pb-lg-0 pe-lg-2">
                        <a class="nav-link" href="{{ path('app_logout') }}">Déconnecter</a>
                    </li>
                {% else %}
                    {# Pour les visiteurs ou utilisateurs enregistrés mais non connectés,
                    les deux liens suivants sont visibles #}
                    <li class="nav-item pb-3 pb-lg-0 pe-lg-2">
                        <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                    </li>
                    <li class="nav-item pe-lg-2">
                        <a class="nav-link" href="{{ path('app_register') }}">Enregistrement</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>